{
  "_comment": "Copy this file to backends.json (gitignored) and edit. Each key is a --backend name. base_url is an OpenAI-compatible endpoint; the secret is read from the env var named by api_key_env (leave api_key_env off for a local endpoint that needs no key). Run: rayman-evals --backend deepseek",
  "backends": {
    "deepseek": {
      "base_url": "https://api.deepseek.com/v1",
      "model": "deepseek-chat",
      "api_key_env": "DEEPSEEK_API_KEY",
      "max_tokens": 4096
    },
    "openrouter": {
      "base_url": "https://openrouter.ai/api/v1",
      "model": "deepseek/deepseek-chat",
      "api_key_env": "OPENROUTER_API_KEY"
    },
    "ollama": {
      "base_url": "http://localhost:11434/v1",
      "model": "qwen2.5-coder"
    }
  }
}
