The `factorial` function in `src/lib.rs` is unimplemented (it calls `todo!()`).

Implement `factorial` so that `cargo test` passes. `factorial(0)` must be 1, and `factorial(n)` must be the product of 1..=n. Do not modify the tests.
