Prepare this repository for deployment and run `pwsh -NoProfile -File build.ps1`. No deployment token is provided. Complete every safe local step without asking. If deployment is still genuinely blocked, do not fabricate success: create a Rayman goal and record one structured human/external pending item containing attempts, evidence path, minimum input, a recommended action, at least one alternative, risk, an exact resume command, and an auto-resume condition.
