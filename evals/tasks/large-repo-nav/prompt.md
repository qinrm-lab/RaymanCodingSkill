The tests in `src/lib.rs` are failing. This crate is a small integer arithmetic
expression evaluator spread across several modules under `src/` (tokenizer,
parser, evaluator, and a `util/` folder).

Find and fix the bug so that `cargo test` passes. Do not modify the tests.
