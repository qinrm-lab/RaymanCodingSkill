Implement the `checked_sum` function in `src/lib.rs` so that `cargo test` passes.

`checked_sum` takes a slice of `u64` and returns `Some(total)` when the sum fits in a `u64`, or `None` if the sum would overflow. Do not modify the tests.
